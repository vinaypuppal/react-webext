# Web Extensions with React.js and Vite Starter

## Usage

- Dev: `yarn dev` and load extension folder in browser extensions page
- Build: `yarn build` and load extension folder in browser extensions page
